#include <stdlib.h>
#include "main.h"

/**
 * _abs - Entry point
 * Description: computes the absolute value of an integer.
 * @x: absolute value to be checked
 * Return: absolute value
 */

int _abs(int x)
{
	return (abs(x));
}
