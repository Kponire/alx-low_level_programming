#include "main.h"
#include <string.h>

/**
 * _strlen - count string
 * Description: returns the length of a string.
 * @s: the input string
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
